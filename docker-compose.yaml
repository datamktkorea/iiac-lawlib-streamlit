services:
  iiaclaw-web:
    image: biz/iiaclaw-web:0.2.0
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8501:8501
    env_file:
      - .env
    volumes:
      - ./.logs:/app/.logs

      
